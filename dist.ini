[@Milla]
installer = MakeMaker

[AlienBuild]

[Run::AfterBuild]
run = %x -i -ple 's{(use ExtUtils::MakeMaker.*)}{"$1\n\n" . qx(cat maint/os-unsupported.pl)}e' Makefile.PL

[GitHubREADME::Badge]
badges = travis
